import { TextStyle } from "react-native"
import { colors } from "./colors"

export const textStyle: TextStyle = { fontFamily: "KGSecondChancesSolid", fontSize: 20, color: colors.white, textAlign: "center" }
